16.08.2014

Хеш-функцию. встроить в цикл for ,например для строки "мама" ,во врема вычисления хеш-функции будут вычислены для "мам","ма","м"

Механизм проверяющий будущие пердсказания на основе прошлых => Как человек коопается в своем прошлом пытаясь понять почему он не так поступил,можно систему предсказания будущего натравливать на прошлое

Можно обернуть coeff в цикл для упрощения кода

Pattern можно убрать из объекта на уровень выше (сэкономит место)

Надо отвязать объекты от фона и от одного цвета ,пока недстаточно правильная ормула создания объектов

Можно будет сделать QT оболочку

Перевести передачу файлов на сервер

Сменить Ajax соединение на WebSocket

Добавить запись с клавиатуры

Сохранение переменной patterns в файл и обратно -> Подзагрузка

Практическое применение

Поработать над сайтом -новые версии всего

В отдельную ветку GIT stable версию
ранее

Оптимизировать звук

Сделать СПЗ

1)Сделать Ньютонову интерполяцию

Сохранить библиотеки Ubuntu или заменить их 
